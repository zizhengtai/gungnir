language: cpp
compiler:
    - clang
before_script:
    if [ $TRAVIS_OS_NAME == linux ]; then sudo apt-get update && sudo apt-get install -y clang-3.5; fi
script: cd tests && cmake . && make check
os:
    - linux
